Pivot vers app_agent reussi via une regle sudo trop permissive (GTFOBins).

flag: RootMeUp{sud0_m1sc0nf1g_str1k3s_ag41n_e0a934}

Indice suivant : tous les binaires privilegies ne sont pas dans /usr/bin.
Cherche un SUID que ton groupe peut executer :  find / -perm -4000 2>/dev/null
